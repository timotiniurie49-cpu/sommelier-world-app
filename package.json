{
  "name": "hidden-term-f2d0",
  "version": "1.0.0",
  "private": true,
  "description": "Sommelier World static app + Cloudflare Worker",
  "scripts": {
    "build": "node -e \"console.log('SommelierWorld static build complete')\"",
    "deploy": "wrangler deploy"
  }
}
